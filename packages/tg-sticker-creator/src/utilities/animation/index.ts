import { blink, BlinkAnimationOptions } from "./blink";
import {
  ParticleAnimationOptions,
  particles,
  RandomizableValue
} from "./particles";

export const animation = {
  blink,
  particles,
};

export { BlinkAnimationOptions, ParticleAnimationOptions, RandomizableValue };
